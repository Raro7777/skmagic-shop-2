/**
 * 정적 이미지 경로 단일 출처.
 *
 * public/ 안의 자산을 가리키는 절대 경로. 파일명을 바꾸거나 CDN 으로 옮길 때
 * 여기 한 곳만 수정.
 */

/** SK매직 로고 — 헤더/푸터/카드 등 다용도. SVG 로 선명도 개선 (PNG fallback 보존). */
export const SK_MAGIC_LOGO = "/sk-magic-logo.svg";
